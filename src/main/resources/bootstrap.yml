aws:
  secretsmanager:
    name: kokodo
    region: ap-northeast-1
    prefix: /secret

encrypt:
  key: ${CONFIG_ENCRYPT_KEY}

#hello: ${ENCRYPT_KEY}
#encrypt:
#  key-store:
#    secret: ${CONFIG_ENCRYPT_KEY}
